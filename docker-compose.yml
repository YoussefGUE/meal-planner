version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - JWT_SECRET=mealplanner_key_2026
      - SPOONACULAR_API_KEY=e3b748e6365448d7983983ed8649b641

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend
